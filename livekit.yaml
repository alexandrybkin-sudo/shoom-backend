port: 7880

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true

# API keys are injected via the LIVEKIT_KEYS env var (see .env), so no secrets live in this file.

logging:
  level: info
